# Backend API URL (leave empty in dev to use Vite proxy to localhost:8080)
VITE_API_BASE_URL=

# Obscure ops sign-in path (default /access) — not linked from the public homepage
# VITE_ADMIN_SIGN_IN_PATH=/access
